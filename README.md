In this game we allowed 5 attempts for the user.
and the user guesses the randomly numbers 0 to 9.
if the user guesses the wrong number then we play the while loop and another tries to get the user.
and if the user guesses the right number so we print "Yahoo! Safe unlocked" Aap ne Sahi number guess kr liya.
and "GAME OVER"
